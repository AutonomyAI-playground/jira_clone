import styled from 'styled-components';

// Backlog page uses ProjectPage wrapper from parent, so minimal styles needed here
// This file exists to follow the project pattern of having a Styles.js for each component directory

export const BacklogPage = styled.div`
  /* Page-level styles if needed in the future */
`;
